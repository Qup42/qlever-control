#!/usr/bin/make -f
export PYBUILD_SYSTEM=pyproject

%:
	dh $@ --with python3 --buildsystem=pybuild --with bash-completion

override_dh_auto_build:
	dh_auto_build
	PYTHONPATH=. \
	    register-python-argcomplete qlever \
	    > debian/qlever-control.bash-completion

